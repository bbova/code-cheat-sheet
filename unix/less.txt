DEFINITION:
  view a file with backward or forward movement

OPTIONS:
	q  > exit
	shift <  > to go the the start of the file
	shift >  > to go to the end of the file
	?[some string]  > to search for a string
	arrows up/down  > to scroll through the file

EXAMPLES:
	$ less filename.txt
		#view filename.txt